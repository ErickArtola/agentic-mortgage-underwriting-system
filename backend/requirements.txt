# Backend dependencies — Python 3.11

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# LangGraph (pinned — core orchestration engine)
langgraph==1.0.5

# LangChain ecosystem (unpinned — let pip resolve compatible versions)
langchain
langchain-groq
langchain-community
langchain-text-splitters

# LLM
groq

# Policy retrieval — TF-IDF (no PyTorch / ONNX; fits Render free tier 512 MB)
# chromadb + fastembed were removed: onnxruntime alone uses ~200 MB in RAM.
# sklearn TF-IDF gives equivalent retrieval quality for this small policy corpus.
scikit-learn

# PDF loading
pypdf

# Validation (pinned — Pydantic v2 is a breaking change from v1)
pydantic==2.10.3

# Utilities
python-dotenv
