# Backend dependencies — Python 3.11

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# LangGraph (pinned — core orchestration engine)
langgraph==1.0.5

# LangChain ecosystem (unpinned — let pip resolve compatible versions)
langchain
langchain-groq
langchain-huggingface
langchain-community
langchain-text-splitters

# LLM / embeddings
groq
sentence-transformers

# Vector store (pinned — API changes between major versions)
chromadb==1.4.0

# PDF loading
pypdf

# Validation (pinned — Pydantic v2 is a breaking change from v1)
pydantic==2.10.3

# Utilities
python-dotenv
